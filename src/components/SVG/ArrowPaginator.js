import React from "react";
import {ReactComponent as ReactLogo} from '../../images/icon/arrow-left.svg';

const ArrowPaginator = (props) => {
    return (
        <ReactLogo/>
    )
}
export default ArrowPaginator